# Stitched umbrella chart output — regenerated by CI on demand
/stitched/

# Local Terraform state — never commit
**/.terraform/
**/.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan

# Local docs build output
/site/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
