{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>hyperi-io/renovate-config"],
  "github-actions": {
    "description": "The scaffold templates in src/hyperi_ci/gitops_templates/workflows/ are real workflows that do not live under .github/, so they match none of the manager's default patterns. They shipped on moveable tags for months because BOTH guards - update-versions.py and Renovate - keyed on location. $default keeps the defaults and adds them, so the watchdog sees them too.",
    "managerFilePatterns": [
      "$default",
      "/(^|/)gitops_templates/workflows/.+\\.ya?ml$/"
    ]
  },
  "packageRules": [
    {
      "description": "GitHub Actions on hyperi-ci are pinned by scripts/update-versions.py (the /deps SSOT in config/versions.yaml): SHA-pinned, 7-day cooldown, enforced at commit time by the pre-commit hook. Renovate stays a PASSIVE WATCHDOG here — it still detects action updates and lists them on the Dependency Dashboard (an independent second opinion), but raises no PR unless a human ticks the box, so it never fights the script's pins. Everything else (cargo/pip/npm) follows the org preset.",
      "matchManagers": ["github-actions"],
      "dependencyDashboardApproval": true
    }
  ]
}
